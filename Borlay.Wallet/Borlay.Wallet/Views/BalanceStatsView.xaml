<UserControl x:Class="Borlay.Wallet.Views.BalanceStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Borlay.Wallet.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Height="Auto">
    <Border>
        <StackPanel>
            <ItemsControl ItemsSource="{Binding Balances}">
                <!--<ItemsControl.Template>
                    <ControlTemplate>
                        <Button Content="{Binding ItemName}"/>
                    </ControlTemplate>
                </ItemsControl.Template>-->
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
